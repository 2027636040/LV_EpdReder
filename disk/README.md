# disk —— 内置文件系统镜像内容

此目录内容会在构建时打包为 `fs_root` 镜像并烧录到 Flash。

运行期规划（与需求文档一致）：
- 少量内置样书（TXT）可放此目录
- 大量书籍/字体/图片请使用 TF 卡：
  - `/book`     TXT 书籍
  - `/font`     TTF 字体文件
  - `/pic`      锁屏/开机图片、图标
  - `/incoming` BLE/USB 接收的文件
